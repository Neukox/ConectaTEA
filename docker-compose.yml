services:
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: conectatea
    volumes:
      - conectatea-db-data:/var/lib/postgresql/MAJOR/docker

volumes:
  conectatea-db-data: